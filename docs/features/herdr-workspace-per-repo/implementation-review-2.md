1. **SHOULD — The new Unicode fallback case pins the exception handler but not strict decoding.** Production now correctly uses `errors="strict"` and catches `UnicodeDecodeError` (`src/claude_teams/backends/process_manager.py:2826-2839`). However, `_git_says` simply raises the supplied exception without inspecting the subprocess keyword arguments (`tests/test_backends/test_process_manager_herdr.py:1744-1762`), and the new parametrized case supplies a preconstructed `UnicodeDecodeError` (`tests/test_backends/test_process_manager_herdr.py:1797-1815`). Consequently, changing production back to `errors="replace"` would leave this test green: the fake would still raise independently of the configured decoder. Have `_git_says` record/assert `kwargs["errors"] == "strict"`, or add a focused invocation test that asserts the Git subprocess receives strict decoding. The raised-exception case can remain to pin the fallback/catch half.

2. **SHOULD — The concurrency test is materially stronger but still has a scheduler-dependent mutation window.** The first-list barrier, failure collection, thread-liveness assertion, and `tab create --workspace w1` assertion fix the previous vacuity (`tests/test_backends/test_process_manager_herdr.py:2162-2237`). With the lock removed, though, failure still depends on the losing thread completing its inner re-list during the winner's fixed 0.5-second sleep (`tests/test_backends/test_process_manager_herdr.py:2195-2203`). A sufficiently delayed thread can resume after `w1` is published, observe it, and make the no-lock mutation pass; the reported mutation run demonstrates sensitivity on one schedule, not determinism across loaded CI hosts. Replace the sleep window with an instrumented `file_lock` context manager: let the first entrant proceed, make the second signal an event immediately before it blocks, and have the fake `workspace create` wait for that “second attempted the lock” event before publishing `w1`. Then the correct path completes without sleeping or deadlocking; if the production lock call is removed/replaced, the event is never signalled and a bounded assertion fails rather than accidentally passing. This still tests that the implementation places the lock around re-list plus create, while the project's separate file-lock tests cover OS locking semantics.

The other three prior findings are resolved and pinned: non-`.git` dotted common-dir names are preserved (`src/claude_teams/backends/process_manager.py:2797-2811`, `tests/test_backends/test_process_manager_herdr.py:1787-1794`); session names are encoded into distinct digest lock paths (`tests/test_backends/test_process_manager_herdr.py:2258-2269`); and the sentinel/fresh-server test verifies no listing, legacy `worker@team` workspace labelling, and successful spawn (`tests/test_backends/test_process_manager_herdr.py:2345-2377`). No new production correctness issue was found.

Overall verdict: **CHANGES REQUESTED**
