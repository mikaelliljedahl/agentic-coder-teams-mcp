# Plan review: xhigh-astra-low
Verdict: CHANGES REQUESTED

1. [MINOR] The Codex test matrix is described as a “GPT-5.6-only catalog” in [plan.md:52](/home/mikael/code/github/wt-xhigh-astra-low/docs/features/xhigh-astra-low/plan.md:52), but the existing fixture for `test_gpt56_only_catalog_errors_for_gpt6_tiers` includes `gpt-6-astra` as well as GPT-5.6 models ([test_codex.py:204](/home/mikael/code/github/wt-xhigh-astra-low/tests/test_backends/test_codex.py:204)). The reason to remove `xhigh` from that error matrix is that Astra is present there; with a truly GPT-5.6-only catalog, `xhigh` must still fail because its selected Astra model is absent, as the preceding Luna+Sol-only case intends to assert ([plan.md:51](/home/mikael/code/github/wt-xhigh-astra-low/docs/features/xhigh-astra-low/plan.md:51)). Clarify the fixture as “GPT-5.6 Luna/Sol/Terra plus GPT-6 Astra” and say that the matrix drops `xhigh`; retain the missing-Astra assertion for a catalog without Astra.

The planned source and documentation edits otherwise cover the current stale pair and rationale: the backend mappings/comments are in [codex.py:119](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/backends/codex.py:119) and [pi.py:185](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/backends/pi.py:185); the Codex `medium` docstring is stale at [codex.py:186](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/backends/codex.py:186); and the tool description, README table/rationale/0.87.0 note, and backend example contain the other affected claims ([server_simple.py:3190](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/server_simple.py:3190), [README.md:694](/home/mikael/code/github/wt-xhigh-astra-low/README.md:694), [ADDING-A-BACKEND.md:101](/home/mikael/code/github/wt-xhigh-astra-low/ADDING-A-BACKEND.md:101)). `CLAUDE.md:111` only describes tiers and missing-model behavior generically, so it does not need a mapping edit. The grep found no other tracked xhigh-pair or derived stale-catalog claims outside these areas and the named tests.

The test plan is otherwise red against the current implementation: `resolve_model("xhigh")` currently maps to Sol in both backends ([codex.py:139](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/backends/codex.py:139), [pi.py:200](/home/mikael/code/github/wt-xhigh-astra-low/src/claude_teams/backends/pi.py:200)), so the planned Astra assertion fails today. Exact `(model, effort)` expectations for `resolve_launch` will also reject an incorrect effort such as Astra @ medium or Astra @ xhigh; the stale-catalog checks cover the changed model availability. No tests were run for this review.
