CHANGES-REQUIRED

1. MAJOR — The plan's safety rationale incorrectly treats every canonical watcher as owner-bound. The plan says canonical watchers have owner binding and later says they are "always owner-bound" (`plan.md:19-21`, `:61-63`), but `_watch_argv` appends `--owner-pid`/`--owner-token` only when `process_manager.creation_token(...)` returns a token (`src/claude_teams/server_simple.py:1515-1518`). The supported fallback is explicitly pinned by `test_watch_argv_omits_owner_binding_when_token_is_unavailable` (`tests/test_watch_command_discovery.py:85-98`). In that fallback, a canonical command is unbound and the proposed default change increases its orphan/forgotten lifetime from one minute to thirty minutes. Revise the current-behavior and risk sections to say "owner-bound when a creation token is available," and explicitly accept or mitigate the unbound canonical fallback. This does not require adding an explicit timeout to normal canonical argv: keeping `timeout=None` as the omission contract (`server_simple.py:1508-1520`; `tests/test_watch_command_discovery.py:119-128`) remains the better single-source-of-truth design once the fallback risk is stated accurately.

2. MAJOR — Test case 1 contradicts its own non-blocking strategy. `runner.invoke(app, ["watch", dir])` with no `--timeout` executes the command; on a quiet directory it will wait until the new 1800-second deadline, and the invocation result is not the Typer parameter metadata (`plan.md:70-72`; `src/claude_teams/cli.py:577-579`, `:613-678`). Specify a metadata-only test and do not invoke `watch`, for example: obtain the Click command with `typer.main.get_command(app)`, select `commands["watch"]`, find the option whose `opts` contains `"--timeout"`, and assert `option.default == 1800.0`. This gives a genuine fast RED→GREEN assertion without a 30-minute hang or timing/mocking seam.

3. MINOR — The planned protocol-doc edit would leave the source citations next to both changed defaults stale. The synopsis still points to `cli.py:182-199` (`docs/reference/agent-messaging-protocol.md:1405-1406`) and the default bullet points to `cli.py:186` (`:1453-1456`), while the current command declaration/default are at `src/claude_teams/cli.py:517-552` and `:519-520`. Include those citation repairs in the same documentation edit; otherwise the new value will be right but its evidence will still be wrong.

4. NIT — The repo-wide `60` sweep found two additional explicit watch recipes in `AGENT_UPGRADE_NOTES.md:44` and `:118`. They are the same bounded Codex foreground-loop pattern as `_DISK_CONTRACT_NOTE` (`src/claude_teams/server_simple.py:1584-1588`), not statements of the CLI default, so leaving them at `--timeout 60` is correct. List them under deliberate non-changes so the plan records every watch-related 60-second touchpoint. `README.md:446` and `:615` use symbolic `SECONDS`/`--timeout` wording and need no update.

5. NIT — The assertion that "all existing watch tests pass explicit `--timeout` values" is too broad (`plan.md:42-44`). The blocking CLI invocations in `tests/test_cli_watch.py` and the timing-free helper in `tests/test_watcher_contract.py:51-52` do pass explicit values, but discovery tests intentionally call timeout-less `_watch_argv`/rendering helpers to pin canonical omission (`tests/test_watch_command_discovery.py:59-62`, `:74-82`, `:119-122`, `:140-155`). Reword this to "all existing tests that execute the blocking watcher pass an explicit timeout"; the intended unaffected-suite conclusion is then accurate.
