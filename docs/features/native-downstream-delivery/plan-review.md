# Native downstream delivery — plan review

VERDICT: CHANGES_REQUESTED

Reviewed the draft at `07eaf67` against its stated implementation baseline
`471a175`, the messaging protocol, and the native-session-wake plan, reviews,
implementation report and smoke record. Source references below are to
`471a175` unless explicitly identified otherwise. Concurrent implementation
edits appeared in the worktree during review; this is a review of the draft,
not an approval of those edits. No source code was changed by this reviewer.

## Findings

1. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:128-138,317-318` — A persistent Codex queue invalidates the existing dead-process negative-receipt rule. `delivery.py:610-613` fails confirmation when the child dies; `server_simple.py:2614-2634` settles an absent nonce after death/grace; kill settles an absent nonce **before** terminating the process (`:2677-2684,6116-6123`). None proves that a durable queue entry cannot dispatch on a later load. A caller can receive `failed`, retry the instruction under a new key via resume, then execute both the surviving queue entry and the retry. An original nonce attributes the late execution; it does not deduplicate it. **Fix:** define carrier-aware confirmation/reconciliation/kill semantics. Native queue attempts remain unresolved after process death, record removal and reboot until a receipt or authoritative removal/non-enqueue proof exists. Block unsafe resume retries of the same instruction, including a new key; specify whether different messages may resume a thread with unresolved queued work. If queue cancellation cannot be proven, report that limitation instead of terminal failure. Test enqueue → timeout → child death → negative scan → new-key retry → thread reload, and kill → same-name replacement → old queue dispatch.

2. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:169-171,272` — `post_failed` plus an absent transcript nonce is not proof of non-delivery. The current POSIX poster catches exceptions across `sendall` **and** `shutdown` (`native_wake.py:124-128`): the complete user line may already have been accepted when shutdown fails. A Windows cancellation can likewise race successful completion. Transcript persistence can lag behind either event. Moving the row to pending authorizes a duplicate post/resume. **Fix:** return a structured transport outcome distinguishing failure before any user-message write from an attempted/partial/complete write with uncertain acceptance. Once writing begins, preserve `unconfirmed` unless a documented transport acknowledgment proves rejection; absence alone is insufficient. Never retake a `taken` entry after poster death merely because its nonce is absent. Test full acceptance followed by shutdown failure, timeout racing completion, partial writes, delayed transcript flush, and poster crash after acceptance but before `posted` CAS.

3. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:152-153` — Publishing `offered` before `_mark_attempt_sent` reverses the established crash contract (`server_simple.py:4859-4887`). The child may take/post immediately, while the durable row still lacks its nonce and snapshot; a lead crash or failed sent-row write then leaves a delivered instruction looking unsent and eligible for retry. **Fix:** durably persist the attempt, method, nonce, target snapshot and lease identity before exposing it to the poster. Specify recovery for sent-without-offer: use a durable not-yet-actionable mailbox state or an atomic locked publish protocol whose orphan state is provably unsent. Do not infer that a missing mailbox file means no post occurred. Test crashes and failed persistence at every boundary, including mailbox publish succeeding while the subsequent delivery-store write fails.

4. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:114-119,168-169,181-183` — Putting the receipt marker in the wrapper changes the prompt-file lifetime contract. Existing finalization and reconciliation delete the file on receipt (`server_simple.py:4361-4362,2610-2612`), whereas the current Claude sidecar receipt comes from the file's `tool_result` (`delivery.py:198-218`; protocol `:1205,1353-1375`). Under this plan the wrapper can be recorded before the agent opens the file, so confirmation deletes its task body. Codex currently has no sidecar transport (`server_simple.py:2827-2838`), so this is a new lifecycle, not reuse of an existing Codex one. **Fix:** keep native sidecars pinned after wrapper delivery until a separate file-read acknowledgment or explicit safe release; exclude unresolved durable carriers from age GC and process-death cleanup as well. Define the public meaning of `delivered` for wrappers. Test wrapper receipt → immediate reconciliation → delayed file read, plus queue persistence across reboot/GC and kill.

5. [MAJOR] `docs/features/native-downstream-delivery/plan.md:154-157,266-268` — The retract-versus-take rule is described as a preliminary state check followed by retract, without defining the authoritative result. If the lead reads `offered`, the poster takes it, and retract loses, returning pending still permits a second attempt. A retract write failure or corrupt mailbox creates the same problem. **Fix:** have one locked CAS return `retracted`, `already_taken/posted`, or `unknown/error`; only a **durably successful** offered→retracted transition permits pending/new nonce. Preserve uncertainty for contention, lost writes and malformed state. Fail closed on unreadable/non-object mailbox data, following `delivery_store.py:263-300` and `leases.py:load_leases`. Add deterministic barrier tests for both race winners, CAS persistence failure, and corruption rather than relying only on stress.

6. [MAJOR] `docs/features/native-downstream-delivery/plan.md:145-165,135-138,274-275` — Neither entries nor capability markers identify the target host incarnation/bound transcript, and the poster has no specified lease/generation fence. A lifetime lock proves that *some* server holds a lock; a fresh heartbeat does not prove it is the server for the current child. After holder death, force-clear, resume or name reuse, a surviving/restarted poster can consume an old offer into a different host. Finalization's generation/operation CAS (`server_simple.py:4252-4275`) cannot undo a socket post already made. **Fix:** bind entries and capabilities to session, identity, correlation/incarnation, backend session, host PID/create token, operation and generation. Validate those bindings at take and immediately before post; distinguish delivery generations from stable host incarnation. Define a global lock order and a kill/force protocol that fences offers, retracts offered work, and treats already-taken work conservatively before settlement/removal. Include the CLI force path (`leases.py:force_clear_lease`) and cleanup in the affected files. Test lead death while child poster survives, force racing take/post, host resume, and same-name successor at a reused numeric generation.

7. [MAJOR] `docs/features/native-downstream-delivery/plan.md:166-174,269-270` — A missing marker is explicitly allowed although it supplies no idle proof. Also a `waiting` observation is not an atomic idle edge: a user/native notice can start a turn after the check, or socket input may not trigger the expected running hook promptly. Thus “never lands between tool calls” does not follow from this loop. Edge-only posting can also strand a second entry after a first post is held/dropped and no running→waiting edge occurs. **Fix:** require a current, incarnation-bound affirmative idle marker; define what resets/rearms the edge and what happens when an accepted write starts no turn. Verify real socket input hook ordering and races with manual input/ordinary native wake. If the host offers no atomic idle-only enqueue, document the remaining timing limitation instead of promising it cannot post mid-turn. Test no/stale marker, waiting→running between take and post, accepted-but-refused input, MCP restart while waiting, and two consecutive offers.

8. [MAJOR] `docs/features/native-downstream-delivery/plan.md:80-102,104-105,129` — End-of-prepare selection is too late to implement the stated busy and `replace_if_idle=False` behavior. Current `_prepare` returns before reserving a lease for busy or idle-but-not-replaceable agents (`server_simple.py:4567-4624`). A native pre-enqueue failure discovered after bypassing that gate must not immediately kill a still-busy target via fallback. **Fix:** specify two stages: determine provisional native eligibility before the idle/replacement gate, then revalidate and commit the method under the granted lease. Safe fallback must re-enter the existing idle/replace checks within the original call budget and release/transfer its lease and FIFO ticket explicitly; never call resume directly from a native error branch. Test busy→native failure→still busy, eligibility disappearing while waiting for a lease, `replace_if_idle=False` with native success/failure, and original-budget exhaustion.

9. [MAJOR] `docs/features/native-downstream-delivery/plan.md:209-224,281-283` — F3 omits several requirements already resolved in the predecessor review. The current notifier starts only under the Claude half/platform gate (`server_simple.py:7149-7155`) and exits when the Claude channel is unavailable or `_CLAUDE=0` (`native_wake.py:343-368`). Merely adding a channel kind will not wake a Windows Codex lead or a Codex-only installation. Registration also lacks locking, monotonic clear tombstones, generation-keyed notice/backoff/cache state, revalidation and activation/restart rules. **Fix:** start a notifier when either eligible host channel is enabled and gate each channel independently. Carry forward predecessor R2-B/R3-A/R3-C: generation-preserving clear, synchronized registration snapshots, per-registration notice/cache/backoff state, pre-queue target/generation checks and transition-only catch-up. Specify how automatic registration waits for late binding and how it stops/replaces on host incarnation changes; a startup-only check is insufficient. Test `_CLAUDE=0/_CODEX=1`, Windows Codex, undrained A→B, clear→set, backoff→replacement, delayed binding, restart backlog and a session switch during queue. Do not replace the existing external-member `external_read` notice with the lead's `read_messages` notice when extracting the runner.

10. [MAJOR] `docs/features/native-downstream-delivery/plan.md:175-180,215-218,243-244` — Repeating flags in Claude `build_env` does not solve an MCP environment filter, and does not address spawned Codex F3 at all. Process launchers already merge the parent's environment (`process_manager.py:636-637,1096-1097,1499-1500`); the observed missing MCP flag happened despite ordinary process inheritance. The explicit Claude MCP config contains only identity/launcher variables (`server_simple.py:2704-2723`), and Codex separately constructs MCP identity configuration. **Fix:** after S-2, propagate effective master, downstream and **both half-switch values** through the child MCP configuration/overrides as well as the host process where needed; include Codex nested leads. Do not blindly turn downstream on just because master wake is on, or drop an explicit half disable. Preserve the socket/token scrub in `process_base.py:104-108`. Test generated MCP env on spawn and resume, master-only/downstream-only combinations, `_CLAUDE=0`/`_CODEX=0`, nested Codex leads and a real child's MCP environment. Missing capability may make B safe, but silently making B/F3 unusable does not complete the feature.

11. [MAJOR] `docs/features/native-downstream-delivery/plan.md:195-202,276-279,320` — Cancel-and-drain protects memory lifetime, but does not by itself establish a hard operation deadline. `CancelIoEx` requests cancellation; `GetOverlappedResult(wait=True)` can still wait past the deadline. Cancellation can also lose to a successful write. The plan must retain the buffer, `OVERLAPPED`, event and pipe handle through **all** completion/error paths, including cancellation failure, not just the expected timeout path. **Fix:** define a bounded caller policy separately from completion cleanup. Keep any outstanding operation in a bounded owned object until drained, disable/back off the affected target, and never free its storage or retry its delivery on uncertainty; alternatively retain Windows unsupported until tested cancellation bounds justify the supported guarantee. Specify ctypes signatures/handle widths, immediate `WriteFile` success versus `ERROR_IO_PENDING`, wait failure, completion byte count, `ERROR_NOT_FOUND` cancellation races and event cleanup. Never pass rounded-down zero to `WaitNamedPipeW` after budget expiry (zero means the server's default timeout); retry busy opens only inside the remaining monotonic budget. Test cancellation failure/delayed completion, immediate completion, cancel→success, short write and 64-bit handles. See Microsoft's [CancelIoEx](https://learn.microsoft.com/en-us/windows/win32/api/ioapiset/nf-ioapiset-cancelioex), [cancellation guidance](https://learn.microsoft.com/en-us/windows/win32/fileio/canceling-pending-i-o-operations), and [WaitNamedPipeW](https://learn.microsoft.com/en-us/windows/win32/api/namedpipeapi/nf-namedpipeapi-waitnamedpipew).

12. [MINOR] `docs/features/native-downstream-delivery/plan.md:189-194,297-298` — Server PID equality is a useful fail-closed gate, but the draft states it as an established ownership fact before S-4 proves the real host/pipe arrangement. A Desktop broker or Node-hosted CLI may differ from the expected `claude.exe`, and a cached path check cannot verify a subsequently reopened pipe instance. **Fix:** check successful `GetNamedPipeServerProcessId` on the **same handle used to write**, before auth/token bytes, and fail closed on query failure as well as mismatch. Keep Windows unavailable when CLI/Desktop S-4 cannot prove the supported host mapping; avoid widening to arbitrary ancestors to make it pass. Verify the client-opened handle experimentally (the Microsoft [API page](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-getnamedpipeserverprocessid) describes a CreateNamedPipe-created handle), and add query-failure, pipe replacement and CLI/Desktop host tests.

13. [MINOR] `docs/features/native-downstream-delivery/plan.md:112-122` — The helper does not preserve the current runner's cwd: `CodexMemberWake._queue` uses `Path.home()` (`native_wake.py:624`), not the reported `CODEX_HOME`. The predecessor explicitly fixed this difference (review R2-E; implementation “member-supplied home”). Also a character count does not bound UTF-8 argv bytes on POSIX or quoted UTF-16 command-line units on Windows. **Fix:** keep `CODEX_HOME=home` and `cwd=Path.home()`, calculate the platform budget from the complete encoded/quoted command, and use a sidecar when it is exceeded. Add quote-heavy, non-BMP, long-path and multibyte cases. Discovery/spawn failures that prove no subprocess started may safely fall back; classify them separately from ambiguous execution failures.

14. [NIT] `docs/features/native-downstream-delivery/plan.md:47-51,98-100,253-254` — Most baseline citations are accurate, but the child shutdown is graceful first with a conditional force kill (`server_simple.py:4761-4766`), not an unconditional force kill; `delivery.py:162-195` identifies receipt extractors, while actual confirmation is `:572-616`. The busy verification cross-reference says §5 N3 (Claude idle), but means N2. “Flag-off golden” also leaves master-on/downstream-off compatibility ambiguous, since C/D are intentionally active there. **Fix:** correct these references/wording and spell out separate baseline expectations for master off, master on/downstream off, and half-disabled channels. Preserve missing `method` on flag-off public views even for previously persisted native rows (`delivery_store.py:232-260`).

## Answers to §8

1. **Treat every subprocess non-zero exit as unconfirmed by default.** Stderr is not an enqueue receipt. Permit fallback for local validation/discovery/exec failure proving no child started. Add a CLI pre-enqueue allowlist only after auditing and pinning supported Codex source/version paths; unknown versions/messages remain uncertain. Test “enqueue succeeded, then CLI exits non-zero.”
2. **Use one JSON document and one stable lock initially.** It makes take/retract CAS and ordering easiest to inspect. Add strict validation, fail-closed reads/writes, bounded terminal-entry retention and recovery rules. One file per entry still requires locking/CAS and complicates enumeration and cleanup; adopt it only if measured mailbox volume requires it.
3. **Keep affirmative-idle-only posting for this feature.** Do not add a mid-turn sender option. Remove the missing-marker exemption, verify native-input marker transitions, and state any unavoidable observation-to-post race honestly (finding 7).
4. **Require child confirmation through its host-provided `CODEX_THREAD_ID` and effective `CODEX_HOME`.** Match them to the parent's positively bound record, verify the thread, and tie registration to host incarnation. Parent binding is valuable corroboration, but does not prove this particular MCP server belongs to that thread. If the MCP environment filters the thread id, retain explicit registration/manual fallback instead of guessing.

## Disposition (author, plan v2)

| # | Severity | Disposition | Where |
|---|---|---|---|
| 1 | BLOCKER | ACCEPTED. For `codex_queue` rows, child death, kill, record removal and reboot never make an absent nonce terminal. A row stays `native_unresolved` until the nonce is found or removal is authoritative (`thread/queue/delete`, gated on the new spike S-5). The queued submission id is stored as `carrier_ref`. The N5 barrier blocks every other guaranteed delivery to the target, including a "same instruction, new key" retry. The barrier is keyed on `(session, name)`, so a same-name successor inherits it. There is a documented operator release ("may still execute"). | plan §2.2.3–§2.2.4, tests 4 |
| 2 | BLOCKER | ACCEPTED and implemented at the transport layer. `PostResult` and `PipeResult` carry `write_started`: on POSIX it is set once connected, before `sendall`; on Windows once `WriteFile` is issued. The mailbox states `posting → failed_before_write | uncertain`. Only `failed_before_write` permits a retry, and `uncertain` is never retaken. | 11addf5; plan §2.3.1; tests 5, 8 |
| 3 | BLOCKER | ACCEPTED. The attempt is persisted before the entry is published. "sent without entry" is provably unsent. An unreadable mailbox means unknown. | plan §2.3.2, tests 6 |
| 4 | BLOCKER | ACCEPTED. Native sidecars are pinned: not deleted on receipt, by reconcile or by GC; released only by kill or session cleanup. The public meaning of `delivered` for wrappers is defined. | plan §2.4, tests 9 |
| 5 | MAJOR | ACCEPTED. `retract` is one locked CAS returning `retracted` / `lost_to_take` / `unknown`. It fails closed on corruption. Tests use deterministic barriers. | plan §2.3.1, tests 5 |
| 6 | MAJOR | ACCEPTED. Entries and capability markers are bound to the host incarnation and the backend session, and validated at take and again before the post. The lock order is defined. Kill and CLI force-clear retract `offered` entries. Taken work stays unresolved. | plan §2.3.1, §2.3.3–§2.3.4, tests 7 |
| 7 | MAJOR | ACCEPTED. Idle must be affirmed (no missing-marker exemption). One post per `running→waiting` edge, with an explicit re-arm. Accepted-but-no-turn becomes `uncertain`. The residual race is documented instead of promising no mid-turn post. | plan §2.3.3, tests 7 |
| 8 | MAJOR | ACCEPTED. Two-stage selection: provisional eligibility before the idle/replace gate, then commit under the lease. The fallback re-enters the existing gates within the original budget and never resumes directly from a native error branch. | plan §2.1, tests 2 |
| 9 | MAJOR | ACCEPTED. The notifier starts when either channel is eligible, and each channel is gated independently. R2-B, R3-A and R3-C carry over. Registration is bound to an incarnation. The external-member notice text is unchanged. | plan §2.6, tests 10 |
| 10 | MAJOR | ACCEPTED. The effective master, downstream and half values propagate through the child MCP config (Claude `--mcp-config` env, Codex `-c` overrides) and are never turned on implicitly. S-2 runs on Linux (the Windows CLI is not logged in). | plan §2.7, tests 11 |
| 11 | MAJOR | ACCEPTED and implemented. The post-cancel drain is bounded; if cancellation lost to a completed write, the completion decides the outcome. An undrained operation is parked with its handle, event, OVERLAPPED and buffer, and reaped later. `WaitNamedPipeW` is never called with 0 remaining. Covered by a scripted-kernel32 suite. | 11addf5, `tests/test_winpipe_logic.py` |
| 12 | MINOR | ACCEPTED. The owner query runs on the writing handle before any byte and fails closed (implemented). Spike S-4 proved live that the client-opened handle reports the CLI `claude.exe` (= `CLAUDE_PID`, the nearest match) under Claude Desktop. The standalone CLI stays for N6. | 71b5e7a/11addf5, spikes.md S-4 |
| 13 | MINOR | ACCEPTED. `CODEX_HOME=home` with `cwd=Path.home()`. The budget is computed on the encoded full command. Discovery and exec failures are classified separately. | plan §2.2.1, §2.2.3, §2.2.6 |
| 14 | NIT | ACCEPTED. The shutdown wording and the `confirm_delivery` citation are corrected. The busy smoke is renumbered to N2. There is an explicit baseline table, and `method` is hidden when the master flag is off. | plan §0, §1, §5 |

§8 answers: all four recommendations adopted. For Q1, every non-zero exit is
uncertain. Q2: one document and one lock. Q3: affirmative idle only, no
mid-turn option. Q4: the host-provided `CODEX_THREAD_ID`, corroborated by
parent binding and bound to an incarnation.

Counts: **4 BLOCKER, 7 MAJOR, 2 MINOR, 1 NIT**. Approval requires the carrier
recovery, publication and file-lifetime rules above to be specified before the
proposed tests can meaningfully prove no duplicate dispatch. Nonce receipt
confirmation establishes presentation of a message, not exactly-once execution
of all actions requested by that message.

## Round 2

VERDICT: CHANGES_REQUESTED

Reviewed v2 at `976ea09`, spikes S-1/S-4, and implemented F5 at `71b5e7a`
and `11addf5`. References in this round are to those current files, except
where explicitly marked baseline. Dispositions are assessed as design fixes;
A/B/D have not yet been implemented, so “resolved” does not mean their tests
or live gates have passed. Source code was not modified.

### Round 1 finding status

| # | Status | Verification |
|---|---|---|
| 1 | **Partially resolved** | Durable queue rows now survive death/removal, all ambiguous subprocess outcomes stay unresolved, and S-5 gates removal (`plan.md:155-197`). However N5 is still specified as an eligibility failure that selects resume, contradicting its barrier; R2-1. Old-target scanning also needs R2-9. |
| 2 | **Resolved at the design level** | Mailbox states distinguish pre-write failure from uncertain writes (`plan.md:228-239`). POSIX preserves uncertainty after send/shutdown failures (`native_wake.py:138-151`); Windows normally preserves it after pending I/O. The exceptional lifetime path is separately R2-8. |
| 3 | **Partially resolved** | Persist-before-publish fixes the original ordering (`plan.md:243-246`), but the new missing-entry recovery rule is not a valid proof without publication/retention fencing; R2-2. |
| 4 | **Partially resolved** | Receipt and GC no longer delete native sidecars (`plan.md:304-315`), but kill/session cleanup still can delete the body of an unresolved durable item; R2-3. |
| 5 | **Resolved at the design level** | Retract has an authoritative, durable CAS result and fails closed (`plan.md:225-239`); deterministic contention/corruption tests are specified (`:456-460`). |
| 6 | **Partially resolved** | Host/session fields and lock order are added (`plan.md:219-221,258-273,293-302`), but the generation predicate accepts fenced work and cleanup/recovery remains unsafe; R2-2/R2-4. |
| 7 | **Partially resolved** | Missing markers are rejected and the observation/post race is honestly documented (`plan.md:274-287`). A latest-value poll cannot reliably observe the required running edge, and the state machine omits timeout/restart semantics; R2-5. |
| 8 | **Partially resolved** | Provisional selection precedes the idle gate, and fallback returns through that gate (`plan.md:98-122`). N5 must not share this fallback branch (R2-1); releasing a lease currently drops the ticket that v2 says it keeps (R2-11). |
| 9 | **Partially resolved** | Independent channel start/gating and monotonic registration transitions are specified (`plan.md:346-375`). Session-specific state, pre-binding eligibility and resume prompt registration are still incomplete; R2-6. |
| 10 | **Resolved at the design level** | Explicit Claude/Codex MCP propagation includes all flag values and preserves socket scrub (`plan.md:379-390`). S-2 is explicitly blocked on Windows and still required; no live propagation proof is claimed. |
| 11 | **Partially resolved** | Bounded cancellation drain, parked storage, completion-vs-cancel handling, pointer-sized structures and nonzero busy waits are implemented (`winpipe.py:51-58,129-148,207-242`). Parking is unbounded and exceptional unwinding is unsafe; R2-7/R2-8. |
| 12 | **Resolved for the tested host arrangement** | Owner-query failure/mismatch closes the same writing handle without a write (`winpipe.py:267-272`). S-4 proves the client-opened handle reports the nearest CLI host under Desktop (`spikes.md:73-99`). Standalone CLI and real auth/user delivery remain N6/S-3 gates, not completed evidence. |
| 13 | **Partially resolved** | Runner cwd and encoded command budgeting are corrected (`plan.md:136-139,203-207`); POSIX additionally needs the per-argument limit and environment accounting, R2-12. |
| 14 | **Partially resolved** | Shutdown/confirmation citations, N2 and the baseline table are fixed (`plan.md:31-38,61-70,510`). One baseline settlement statement remains inaccurate, R2-13; the flag-off safety conflict belongs to R2-1. |

### Findings

1. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:105-122,179-196,431-437` — N5 is a **global safety barrier**, but it is included among native eligibility conditions and test 2 explicitly says each N0–N5 failure selects `resume`. Stage 2 likewise turns lost eligibility into resume. That reintroduces the exact queue→resume duplicate that the barrier was added to prevent. Also neither master/downstream off nor a disabled half may bypass unresolved native state; existing durable work remains dangerous after flags change. `pending_delivery` alone cannot implement this barrier: it is a single agent field, is written only after finalization, disappears with record removal, and its baseline scanner checks for a found nonce rather than rejecting every other attempt. **Fix:** check unresolved rows for `(session, target)` across all senders/phases under the target serialization protocol, before either carrier is selected and again under the granted lease. N5 false always returns the stated pending barrier; it never selects resume. Keep it active independently of transport-enable flags and include `sent` rows whose queue call/ref write/finalization is still in flight. Add native→disable flag→retry, N5 changing between stages, holder crash after enqueue before ref/finalization, and same-name replacement tests. Explain that baseline byte identity applies to sessions without native recovery state. Correct N5 smoke `:513`: an ordinary resume cannot both be blocked and dispatch the item; distinguish an explicit operator action or independent host reload.

2. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:243-252,297-302,461-463` — “Sent without entry means provably unsent” is unsafe as written. After sent is persisted but before publish, an active reconciler can observe no entry and set pending while the original holder is still able to publish it. More seriously, cleanup may remove a mailbox containing `posted` entries (`:301-302` excludes only taken/posting/uncertain); an accepted post with a delayed receipt then becomes indistinguishable from never-published work. The kill rules also enumerate taken/posting/uncertain but omit posted-without-receipt. **Fix:** make missing-entry recovery conditional on exclusion of any live/in-flight publisher and a durable publish-intent/tombstone protocol. Preserve evidence for every published nonce until its delivery row is durably settled, including posted entries. Inspect receipts before deciding recovery, and treat posted-without-receipt as unresolved. Define mailbox→delivery-store ordering for cleanup and persistent mailbox absence/corruption; deletion is not a non-delivery acknowledgment. Test live publisher paused between sent/publish versus `delivery_status`, and successful socket write→posted→kill cleanup→delayed transcript receipt→retry. Ensure recovery cannot set pending and then allow the old holder to publish.

3. [BLOCKER] `docs/features/native-downstream-delivery/plan.md:164-197,304-315,477-479` — Native files are still released by kill/session cleanup even though a Codex queue entry explicitly survives kill/removal/reboot. A later reload presents the wrapper with a deleted task body. The defined `delivered` meaning (“body available”) can therefore be false. A Claude post already buffered by the host, or an undrained pipe write, has the same risk if kill cannot prove that host terminated. **Fix:** pin sidecars to carrier disposition, not registry lifecycle. Keep a durable file reference reachable from the row; only authoritative cancellation, confirmed body-read acknowledgment, or a documented explicit discard may release it. Session cleanup must retain/export unresolved carriers and their payloads or require an explicit destructive resolution. Test kill→thread reload→file read, unproven-host kill, parked pipe completion after cleanup, and session cleanup with unresolved wrappers. Receipt-only pinning is correctly fixed; lifecycle pinning is not.

4. [MAJOR] `docs/features/native-downstream-delivery/plan.md:219-221,268-273,293-302` — `current_generation >= entry.generation` accepts work **after** force has advanced the fence; it is the opposite of a revocation check. Generation is also not checked against an operation identity or a stable dispatch epoch. Simply changing `>=` to `==` is insufficient: normal finalization currently bumps generation while an offered entry can legitimately be waiting to post. Entry validation omits an explicit current-record backend-session match and does not specify a persistent posting claim owner. **Fix:** distinguish stable host incarnation, operation identity and a revocable dispatch epoch. Define which normal mutations preserve eligibility and which force/resume/removal mutations invalidate it. Take/begin-post must CAS the exact nonce/state/dispatch identity under the declared lock order; a durably recorded `posting` claim is never replayed by a replacement poster. Validate backend session as well as host token. Define what kill/force does when retract is unknown, and preserve an already-issued post as uncertain. Test force with a surviving host/server, ordinary finalize before delayed take, MCP restart in taken/posting, and a backend-session change without a new numeric generation. Finalization fencing cannot undo a post.

5. [MAJOR] `docs/features/native-downstream-delivery/plan.md:274-282,464-473`; `src/claude_teams/hooks.py:63-81,84-119` — One latest-value marker cannot reliably demonstrate a running→waiting edge. A short turn can write both states between polls, leaving the poster seeing only waiting forever. A restart while already idle has no observed running edge at all. The marker currently contains state/event/time and a random per-write `gen`, rather than the promised host-incarnation binding or a retained turn sequence, and `posted → uncertain after RENOTIFY` is missing from the transition table (`plan.md:228-235`); an already-receipted item must not be relabeled uncertain merely because running was not observed. **Fix:** persist an incarnation-bound monotonic turn/idle sequence in the hook marker (or an equivalent durable edge record), and persist the last consumed idle sequence. Specify the first idle eligibility, restart behavior, failed-before-write rearming and transitions for accepted-without-receipt; receipt always wins. Use “no evidence of a turn” rather than claiming no turn started from a missed poll. Include hooks/marker wiring among affected files and test running+waiting entirely between ticks, poster restart on the same waiting marker, stale predecessor marker, pre-write failure and successful receipt without an observed running marker.

6. [MAJOR] `docs/features/native-downstream-delivery/plan.md:351-375,480-487` — F3 registration state is keyed by `(identity, generation)` even though files/generations are session-local. Two sessions can both have `team-lead, generation=1`; outstanding notice/backoff/verification from S1 can suppress S2. For spawned leads, accepting and queueing before parent binding exists permits a wrong supplied thread to be woken before the later mismatch clears it. New host incarnations ignore registrations, but only the **spawn** prompt instructs registration; a resumed nested lead gets no specified re-registration instruction. **Fix:** key owner/notice/cache/backoff/catch-up state by `(session, identity, registration_generation, host_incarnation)`. Store pre-binding registrations as provisional and do not queue until the required corroboration succeeds (unless S-6 supplies independently verified current-host proof). Add the registration instruction to resume/tool recovery text as well as spawn, with the effective Codex home check. Test S1→S2 with equal generations and unread backlog, provisional wrong thread receiving zero queues, binding delayed until after first ticks, MCP restart under the same host and a resumed nested lead under a new host. Preserve the existing external-member notice behavior.

7. [MAJOR] `src/claude_teams/winpipe.py:169-204,233-237,259-262`; `src/claude_teams/native_wake.py:450-456` — Parking keeps storage alive correctly on the normal timeout path, but there is no bound or per-channel exclusion. Each retry can add another pending operation, buffer and two handles to `_PARKED`. Wake backoff bounds frequency, not accumulated resources, and neither `post` nor notifier prevents a second write while the first is still capable of completing late. Reaping happens only on another post. Using the existing scripted kernel with unsignaled waits, two calls to the same pipe produced **two parked entries and two WriteFile calls**. **Fix:** retain parked operations by channel/host incarnation and refuse or disable additional writes while one remains undrained, with a process-wide resource ceiling. Add an explicit bounded reaper lifecycle independent of new payload posts. Preserve uncertain delivery state and report late completion without treating it as cancellation proof. Test repeated retries, concurrent callers, other channels remaining usable, shutdown, late completion after a target/session switch and exhaustion of the global cap. This is the missing bounded-owned-operation policy from round-1 #11.

8. [MAJOR] `src/claude_teams/winpipe.py:207-242,259-274` — Pending-I/O storage is not exception-safe. An exception after `WriteFile` but before the explicit parked path lets `_Pending` unwind, while `post`'s `finally` closes the pipe handle; closing does not establish completion before freeing the OVERLAPPED/buffer. Concrete scripted reproduction: `post(..., deadline=float('nan'))` issued `WriteFile`, then `_remaining_ms` raised `ValueError`; result was uncertain, the handle was closed, the event was not closed, and `_PARKED` remained empty. Normal configuration validates finite deadlines, but a timing/FFI exception or allocation failure after initiation has the same lifetime requirement. **Fix:** validate finite positive deadline/payload bounds before opening or writing, and give each pending operation an exception-safe owner that cancels/drains or retains storage on every post-initiation exit. Do not rely on the broad outer exception handler to make kernel memory use safe. Inject exceptions from wait, cancel and completion, and test invalid deadlines produce zero writes. Microsoft's [WriteFile](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-writefile) and [CancelIoEx](https://learn.microsoft.com/en-us/windows/win32/api/ioapiset/nf-ioapiset-cancelioex) require the storage to remain valid until completion. Preserve immediate success, cancel-lost-to-success and same-handle owner checks, which are correctly implemented.

9. [MAJOR] `docs/features/native-downstream-delivery/plan.md:144-153,173-176,192-201`; `src/claude_teams/server_simple.py:2566-2585` — Retaining `target_snapshot` does not mean current reconciliation uses it: `_scan_target` prefers any live registry record with that name. After kill and a same-name successor, the old queue's receipt can land in the old thread while all scans use the successor's transcript. N5 then blocks permanently, and S-5's negative scan can inspect the wrong thread. The second `carrier_ref` write also needs a defined attempt CAS so a delayed update cannot overwrite a receipt settlement/operator action or attach an old queue id to a new attempt. **Fix:** freeze the original backend session, home and transcript binding per native attempt and always reconcile that attempt against that carrier; use the current record only for matching-incarnation liveness. Update the returned submission id under `(sender, key, nonce, operation_id)` CAS without reverting terminal state; missing/unparsable/ref-write failure remains unresolved and cannot authorize deletion. Test queue receipt before ref persistence, ref-write failure, operator action racing ref write, and kill→same-name successor→old-thread receipt. Document that no `carrier_ref` means S-5 cancellation is unavailable.

10. [MAJOR] `docs/features/native-downstream-delivery/plan.md:105-113,131-134,498-510`; `docs/features/native-downstream-delivery/spikes.md:49` — V2 remains explicitly unmeasured, but v2 removes v1's conditional busy-Codex restriction. S-1 proved an **idle** TUI presentation and metacharacter handling; it did not establish that queueing during a turn dispatches after that turn. **Fix:** keep a merge gate requiring N2 on supported TUI/Desktop arrangements, or retain the provisional busy-native-disable/idle-only policy until it passes. State the behavior when N2 fails and test that fallback uses the idle gate rather than silently selecting busy queue delivery. Do not describe S-1 as evidence of busy behavior.

11. [MINOR] `docs/features/native-downstream-delivery/plan.md:119-122`; `src/claude_teams/leases.py:423-435` — “Release the lease and keep its FIFO ticket” is not what the current release operation does: it removes the holder's waiter. Passing that ticket to `reserve_lease` later puts it at the tail. **Fix:** specify whether fallback deliberately relinquishes its queue position, or add an atomic downgrade from active holder to persistent waiter with the same ticket. Never hold the lease while waiting for an idle gate that needs another operation to progress. Test two callers around native eligibility loss/pre-enqueue failure, ticket position/order and failed persistence of the downgrade.

12. [MINOR] `docs/features/native-downstream-delivery/plan.md:136-139` — `ARG_MAX/4` is not a valid universal POSIX single-message argv cap. Linux can reject one argument at its per-string limit even when aggregate argv is below that threshold; exec also accounts for environment and terminators. That turns a large inline request into an avoidable Popen failure/retry loop. **Fix:** use a conservative per-argument ceiling plus the total argv/environment budget, including nulls and pointers where relevant, or choose a smaller tested fixed inline limit and sidecar above it. Handle an unavailable `sysconf` value conservatively. Test a single multibyte argument over the platform string limit and a large inherited environment. See Linux's primary [execve manual](https://man7.org/linux/man-pages/man2/execve.2.html).

13. [NIT] `src/claude_teams/winpipe.py:5-8,248`; `docs/features/native-downstream-delivery/plan.md:71-74` — The module introduction still promises no operation or late write after return, which parking intentionally does not guarantee; the function docstring correctly says deadline **plus** bounded drain. Also the baseline kill path settles an absent nonce before killing, without waiting for the dead-child grace (`server_simple.py:2677-2684,6116-6123`), whereas v2 describes reconciliation and kill together as dead-plus-grace. **Fix:** align the module documentation with parked uncertainty and its bounded-resource policy, and describe the two baseline settlement paths separately. These are documentation corrections, not reasons to restore unbounded draining.

### Validation and remaining gates

`uv run pytest tests/test_winpipe.py tests/test_winpipe_logic.py
tests/test_native_wake.py -q`: **68 passed, 4 skipped** on this Windows
worktree. The real pipe tests passed; the four skips are platform-marked
cases in the selected suites. The two scripted probes above made no real
host/socket writes. Tests currently cover one parked operation, not repeated
or concurrent parking, and do not inject exceptions after I/O initiation.

S-4 validates ownership under Desktop, not auth/user ingestion or guaranteed
delivery; S-2/S-3, standalone CLI N6, busy N2, D's S-6 and optional cancellation
S-5 remain live gates. This review does not claim they passed.

Round-2 counts: **3 BLOCKER, 7 MAJOR, 2 MINOR, 1 NIT**. The normal F5
completion/owner-check paths are substantially improved, but the remaining
carrier-recovery and pending-I/O lifetime issues require changes before approval.

## Round 2 disposition (author, plan v3)

| # | Severity | Disposition | Where |
|---|---|---|---|
| R2-1 | BLOCKER | ACCEPTED. N5 is removed from eligibility and becomes a barrier that runs first, before any carrier choice, and again under the lease. It is independent of the flags. It queries the delivery store across all senders and phases, including in-flight `sent` rows, and never falls through to resume. The baseline byte-identity claim is limited to sessions without native recovery state. The N5 smoke uses an independent host reload or an operator release. | plan §0, §2.1, §2.2.4; tests 1–2 |
| R2-2 | BLOCKER | ACCEPTED. Publishing is a CAS that checks the row is still `sent` with the same operation and that no tombstone exists. Recovery of a row with no entry requires the holder to be `not_ours` and writes a `revoked` tombstone before `pending`. Every entry, including `posted`, is retained until its row is terminal. Cleanup reads the delivery store before writing the mailbox. Posted-without-receipt stays unresolved. Receipts are inspected first. | plan §2.3.2–§2.3.3; tests 6–7 |
| R2-3 | BLOCKER | ACCEPTED, by removal. Native carriers carry inline text only, up to 16 KiB. Larger messages use resume and its existing sidecar lifecycle, so no native attempt depends on a file body. | plan §2.4 |
| R2-4 | MAJOR | ACCEPTED. The plan now separates the host incarnation, a revocable `dispatch_epoch` (bumped only by kill, force, resume, removal and re-spawn, never by native finalisation) and the operation identity. Take and begin are CAS operations on nonce, state and epoch, with a backend-session check. A durable `posting` claim is never replayed. An `unknown` retract keeps the row unresolved. | plan §2.3.1–§2.3.5; test 8 |
| R2-5 | MAJOR | ACCEPTED. The hook marker gains `backend_session_id` (from the payload), a monotonic `idle_seq` and a `turn_seq`, written under a lock. The poster persists `consumed_idle_seq` and posts only when `idle_seq > consumed`. First-post, restart and pre-write-failure re-arm rules are specified. A receipt always wins, and there is no `posted→uncertain` relabel. `hooks.py` is added to the files. | plan §2.3.4; tests 8–9 |
| R2-6 | MAJOR | ACCEPTED. State is keyed by `(session, identity, generation, host incarnation)`. A spawned lead's registration is provisional and never queued until the parent binding corroborates it. The registration instruction is added to the resume prompt and the recovery text. | plan §2.6; test 11 |
| R2-7 | MAJOR | ACCEPTED and implemented (16fdd4f). A path with a parked write refuses new writes (`channel_busy`). The process-wide cap is `MAX_PARKED`. The notifier reaps on every tick. Tests cover repeated retries, the cap and reaping. | `winpipe.py`, `tests/test_winpipe_logic.py` |
| R2-8 | MAJOR | ACCEPTED and implemented (16fdd4f). Every exit after `WriteFile`, exceptions included, either observes completion or parks. Deadlines that are not finite or not positive are refused before open. Tests inject a wait exception and invalid deadlines. | same |
| R2-9 | MAJOR | ACCEPTED. The carrier (backend session, home, rollout or transcript path, epoch, host) is frozen per attempt, and reconciliation always scans that carrier. The `carrier_ref` CAS keys on `(sender, key, nonce, operation_id)` and never reverts terminal state. A missing ref means S-5 cancellation is unavailable. | plan §2.1, §2.2.2, §2.2.4; tests 4–5 |
| R2-10 | MAJOR | ACCEPTED. E6 limits native Codex delivery to idle targets until N2 passes live. If it passes, E6 is lifted in this PR; if it fails, E6 stays and the result is recorded. S-1 is no longer cited as busy evidence. | plan §2.1, §5 |
| R2-11 | MINOR | ACCEPTED. The fallback deliberately gives up the lease and its FIFO position; this is documented and the order is tested. The lease is never held while waiting at the idle gate. | plan §2.1; test 3 |
| R2-12 | MINOR | ACCEPTED. There is a fixed inline limit of 16 KiB UTF-8, well below the per-string and command-line limits; above it, resume is used. | plan §2.4 |
| R2-13 | NIT | ACCEPTED. The module docs are corrected (16fdd4f). The two baseline settlement paths are described separately. | plan §1 |
